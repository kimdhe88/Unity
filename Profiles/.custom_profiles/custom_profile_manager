source $(dirname -- "$(readlink -f -- "${0}")")/.env.sh

cpm_module=${customProfileDirectory}/.custom_profile_manager

profilestatus(){
  sh ${cpm_module}.profilestatus
}

useprofile(){
  sh ${cpm_module}.useprofile
}
