source $(dirname -- "$(readlink -f -- "${0}")")/.env.sh

cpm_module=${customProfileDirectory}/.custom_profile_manager

profilelist(){
  sh ${cpm_module}.profilelist
}

enableprofile(){
  sh ${cpm_module}.enableprofile ${1}
}

disableprofile(){
  sh ${cpm_module}.disableprofile ${1}
}
